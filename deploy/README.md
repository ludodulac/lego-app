# Deployment assets

- `render.env.example`: safe variable-name template only; never commit real secret values.
- Root `render.yaml`: Render Blueprint for the FastAPI API.
- Root `Dockerfile`: portable container deployment path.

See `docs/DEPLOY_RENDER.md` for the first live deployment procedure.
