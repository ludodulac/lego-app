# Interface restore

The photo interface is restored to the repository state before the mobile shell work. Survey and backend fixes remain unchanged. A regression test prevents the competing mobile shell layer from being loaded again.
